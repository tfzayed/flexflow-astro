---
import { markdownify } from "@/lib/utils/textConverter";
import type { CollectionEntry } from "astro:content";
import Card from "../Card.astro";

type ServiceType = CollectionEntry<"features">["data"];

const { service } = Astro.props as ServiceType;
---

<section>
  <div class="section">
    <div class="container">
      <div class="text-center mb-11">
        <span
          class="bg-secondary/20 px-5 py-1 rounded-full text-base-sm text-dark inline-block mb-3"
          data-aos="fade-up-sm"
          set:html={markdownify(service.badge)}
        />

        <h2
          class="mb-4"
          data-aos="fade-up-sm"
          data-aos-delay="50"
          set:html={markdownify(service.title)}
        />
        <p
          class="mb-7 text-dark/75 lg:col-8 px-10 mx-auto font-medium"
          data-aos="fade-up-sm"
          data-aos-delay="100"
          set:html={markdownify(service.content)}
        />
      </div>
      <div class="grid md:grid-cols-2 xl:grid-cols-3 gap-5">
        {service.services.map((s, i: number) => <Card card={s} i={i} />)}
      </div>
    </div>
  </div>
</section>
